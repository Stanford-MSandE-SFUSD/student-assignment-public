# Dist B (SI): 3 bands 0–0.75, 0.75–2, 2+ miles; main-text FRL 50% reserves.
# Paper: "distance priority alternative B".

assignment-algorithm: DA
ctip-options:
- 1

restrict-zone: false
guard-rails: 1
reserve-settings: {
  "column": "freelunch_prob",
  "thresholds": "percentile:50",
  "lower_disadvantaged": true,
  "reserve_fraction": [0.5, 0.5]
  }
non_designation_boost: 128
soft_reserve_boost: 64

policies:
- Con1

priority-weights:
  ctip: 8
  sibling: 16
  zone: 0
  distance: 4
  language-programs:
    lp-sibling: 16
    lp: 8
    sibling: 4
    ctip: 2

distance-priority:
  thresholds: [0.75, 2]

sibling-access: true
designate: true
zone-building-blocks: 'attendance_area'
ties-options:
- MTB
