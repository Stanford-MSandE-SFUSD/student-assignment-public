# Dist A + Reserves B (income 95,292).
# SI: alternative distance priorities × Reserves B.

assignment-algorithm: DA
ctip-options:
- 1

restrict-zone: false
guard-rails: 0
reserve-settings: {
  "column": "median_hh_income",
  "thresholds": [95292],
  "lower_disadvantaged": true,
  "reserve_fraction": [0.57, 0.43]
  }
non_designation_boost: 128
soft_reserve_boost: 64

policies:
- Con1

priority-weights:
  ctip: 8
  sibling: 16
  zone: 0
  distance: 4
  language-programs:
    lp-sibling: 16
    lp: 8
    sibling: 4
    ctip: 2

distance-priority:
  thresholds: [0.5, 1]

sibling-access: true
designate: true
zone-building-blocks: 'attendance_area'
ties-options:
- MTB
